# WIRE-R9 匯入驗證

日期：2026-09-14。

| 驗證 | 結果 |
|---|---|
| `npm test` | 101 項通過、0 失敗、0 跳過，含 strict TypeScript |
| 匯出／匯入 | 原始路徑及 W／E 編號不變；持續狀態、電源設定與視角資料保留；新增接線不重複編號 |
| 操作板 | 收合快照原樣還原，之後可再次開闔且端點保持 |
| 模擬來源 | running／halted 快照均以 off 匯入，不重播供電結論；重新送電使用還原的實際接線求解 |
| 錯誤保護 | 不合法 JSON、版本／配置／型號／端子／狀態、重複線、壞座標與不連到端子的路徑均拒絕；原狀態、姿勢、線路及 mesh 不變 |
| 頁面及模式 | 目前仍送電時拒絕；整盤／單獨檢視不互相覆蓋；MC1 附掛狀態可還原 |
| 匯入事件 | 等待檔案讀取、重複提交防護、同檔再選、讀取／套用失敗、大小限制及持續錯誤訊息通過 |
| 接線工作階段 | 還原選中線與跨 W／E 的復原順序，清除未完成起點 |
| `npm run build` | 成功；網站與離線 HTML 同為 WIRE-R9，內嵌程式語法及逐位元組一致，四張銘牌材質內嵌 |
| 唯讀審查 | 獨立審查通過，未發現可行動的正確性或安全性問題；另跑 13 項相關測試及 strict TypeScript |

以上使用 Node 中真實 Three.js 模型、狀態控制器與最小 DOM 事件介面。**瀏覽器 E2E 尚未通過**：既有受管瀏覽器的 WebGL 停用阻擋見 [E2E 記錄](electrical-acceptance.md)，本次未將 Node 結果視作瀏覽器驗收。

待可用 WebGL 瀏覽器補驗：由實際檔案選擇器載入 WIRE-R8／R9、目視相機與選取、匯入後操作元件／接線／開闔／重新送電，以及網站與離線版的檔案讀取。
